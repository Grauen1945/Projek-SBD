SELECT Nama
FROM pengguna
WHERE ID_Pengguna IN 
(
    SELECT ID_Pengguna
    FROM pendaftaran
    WHERE ID_Kursus =
    (
        SELECT ID_Kursus
        FROM detail_kursus
        WHERE Nama_Kursus = 'Software Development'
        )
);
